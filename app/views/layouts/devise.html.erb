<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Rails3 Bootstrap Devise Cancan" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Training assembler" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>" style="background-image:url('/jogging.jpg'); background-size:cover; padding-top:0">
    
    <div id="main" role="main" class="canvas">
      <div class="container">
        
           
              <%= render 'layouts/messages' %>
              <%= yield %>
           
          <footer>
          </footer>
        
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
  </body>
</html>
